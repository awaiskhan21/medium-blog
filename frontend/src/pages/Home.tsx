import LandingPage from "@/components/LandingPage";

function Home() {
  // const navigate = useNavigate(); // Initialize the navigate function
  // const jwt = localStorage.getItem("token");
  // useEffect(() => {
  //   jwt ? navigate("/blogs") : navigate("/signin");
  // }, [navigate]);

  return <LandingPage />;
}

export default Home;
